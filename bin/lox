#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative "../lib/rlox"

if ARGV.length != 1
  warn "Usage: lox [script]"
  exit 64
end

runner = Rlox::Runner.new
result = runner.run_file(ARGV[0])
puts result
